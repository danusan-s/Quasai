#pragma once
/**
 * @file ops.h
 * @brief Linear algebra operations and operators
 * 
 * Implement matrix and vector operations:
 * - Matrix multiplication (gemm)
 * - Element-wise operations (add, subtract, multiply)
 * - Broadcasting for mixed-size operations
 * - Linear algebra utilities (trace, determinant, etc.)
 * 
 * Considerations:
 * - Optimized for performance (cache-friendly)
 * - Support for different matrix layouts
 * - Optional: SIMD optimizations
 */

namespace quasai {

// TODO: Implement operations here
// template<typename T>
// Matrix<T> matmul(const Matrix<T>& a, const Matrix<T>& b);

} // namespace quasai

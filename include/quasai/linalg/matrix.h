#pragma once
/**
 * @file matrix.h
 * @brief Matrix class for linear algebra operations
 * 
 * Implement a 2D matrix class that supports:
 * - Matrix construction and initialization
 * - Element access (row, col)
 * - Basic operations (add, subtract, multiply)
 * - Transpose and other transformations
 * - Matrix-vector multiplication
 * 
 * Considerations:
 * - Support both row-major and column-major storage
 * - Optional: Use Tensor as underlying storage
 * - Memory efficiency for large matrices
 */

#include <cstddef>

namespace quasai {

// TODO: Implement Matrix class here
// template<typename T>
// class Matrix { ... };

} // namespace quasai
